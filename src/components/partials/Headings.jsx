import React from 'react'

const Headings = ({label}) => {
  return (
    <div>
      <h2 className='text-center text-3xl font-bold mb-8 text-gray-800'>{label}</h2>
    </div>
  )
}

export default Headings
